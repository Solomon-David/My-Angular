import { SwitchClassDirective } from './switch-class.directive';

describe('SwitchClassDirective', () => {
  it('should create an instance', () => {
    const directive = new SwitchClassDirective();
    expect(directive).toBeTruthy();
  });
});
